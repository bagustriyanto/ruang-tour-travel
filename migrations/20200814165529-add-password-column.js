'use strict';

const { query } = require("express");

module.exports = {
  up: async (queryInterface, Sequelize) => {
    /**
     * Add altering commands here.
     *
     * Example:
     * await queryInterface.createTable('users', { id: Sequelize.INTEGER });
     */
    await queryInterface.addColumn('credentials', 'password', { type: Sequelize.STRING })
    await queryInterface.addColumn('credentials', 'salt', { type: Sequelize.STRING })
  },

  down: async (queryInterface, Sequelize) => {
    /**
     * Add reverting commands here.
     *
     * Example:
     * await queryInterface.dropTable('users');
     */
    await queryInterface.removeColumn('credentials', 'password')
    await queryInterface.removeColumn('credentials', 'salt')
  }
};
